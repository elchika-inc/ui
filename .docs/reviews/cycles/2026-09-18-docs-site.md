verified_impl_sha: f15c89293bea3afa92f8ea909e3c277ca40f5bbc

# issue #86 ドキュメントサイト意匠 — レビューサイクル

- 実施日: 2026-09-18
- 実装: Codex（gpt-6-astra、Orca worker）。
- レビュアー: Claude Sonnet、fresh context、1レンズ1プロセス、5レンズを順次実行。
- 対象: 実装 commit `f15c89293bea3afa92f8ea909e3c277ca40f5bbc` の担当9ファイル。spec の §1 / §2 / §4 / §A、対象 diff、変更ファイル全文を入力した。確定した司令塔の裁定をレビュープロンプトの境界条件に記載した。
- レビュー入力 SHA-256: `b728eeb187ef346620928c727d2e8817bd93f7971fa092e77e2b9389d2f51482`。記録時に全9ファイルの全文が入力時点と一致することを照合した。
- flag は correctness・セキュリティ・明示要件に影響する確信度80%以上の指摘だけ。optional は分離した。
- 最大3ラウンド、境界マーカー不備は同じレンズを1回だけ再実行する条件で起動した。

```sh
claude -p --model sonnet --tools Read,Glob,Grep --strict-mcp-config --safe-mode --permission-mode dontAsk --no-session-persistence
```

## 第1ラウンド

| レンズ | 試行 | exit | 所要時間 | flag | 結論 |
|---|---:|---:|---|---:|---|
| Fresh Eyes | 1 | 0 | 304.28秒 | 0 | 採用 |
| Security | 1 | 0 | 89.66秒 | 0 | 採用 |
| Core Logic | 1 | 0 | 358.32秒 | 0 | 形式不備のため取り直し |
| Core Logic | 2 | 0 | 428.85秒 | 0 | 採用 |
| Tests | 1 | 0 | 272.85秒 | 0 | 採用 |
| Domain | 1 | 0 | 382.89秒 | 0 | 採用 |

初回ラウンドで5レンズすべて flag 0 のため終了。Core Logic の初回応答は境界マーカーに空白が混入したため指定どおり1回だけ取り直した。2回目にも形式不備がある場合は、その全文を改変せず採用する裁定に従った。実装の修正要求0件、受容した flag 0件。

## optional の扱い

Fresh Eyes のカテゴリ件数ラベルの整列と一覧ページ先頭の青ラベルの提案は、明示要件に影響しない任意提案として採用しなかった。カテゴリ件数は既存の baseline 配置を維持し、一覧の先頭ラベルは §A.4 の変更対象であるカテゴリ eyebrow と分けて既存色を維持した。Core Logic の静的 Sidebar 件数の追加提案は、必須の実ブラウザ検査でサイト用1件を確認済みのため重複追加を見送った。既存の順序テストの `indexOf("elchika-inc/ui")` は変更前から head の title が最初に一致する式であり、今回の2テスト更新の外側にある既存課題として扱った。Tests の `home` prop 整理案は、Sidebar 本体を現状維持する範囲に含まれる既存インターフェースの任意整理として採用しなかった。Domain の hover 背景の提案は、§A.2 が指定する既存 Button outline variant の状態表現を維持する判断で採用しなかった。背景は通常時に透明とし、hover は既存の state-hover を継承する。flag の格下げや握りつぶしは行っていない。

## ACCEPTED_RISKS

レビュー flag の受容は0件。補足観測で見つかった375px幅の既存 install カードの横 overflow は、変更していない本文の既存課題として PR 本文とブラウザ report に記録する。

## 採用した応答全文

先頭・末尾の境界マーカー外の発言も含めて各応答をそのまま保存する。

### Fresh Eyes

````text
advisorの指摘の通り、`src/pages/index.astro` の `title="elchika-inc/ui - はじめに"` により `<head><title>` 内に文字列がヒットするため、そのFLAG候補は成立しませんでした。撤回します。

===REVIEW_BEGIN===
結論: LGTM

[OPTIONAL] src/site/component-index.tsx:153付近 / 確信度 ~60% / カテゴリ見出し行の親 `flex items-baseline` はそのままで、h2 を eyebrow 付きの div でラップしたため、右の「N items」（`<p>`）は div 内最初の行ボックス（11px の eyebrow）のベースラインに揃い、`mt-3` で下にずれた h2 から視覚的に浮く。見た目の回帰で明示要件への影響ではない。 / 修正するなら `items-baseline` → `items-end`、または eyebrow を flex コンテナの外に出す。
[OPTIONAL] src/site/component-index.tsx（一覧ページ見出しの `Component index` eyebrow） / `text-primary` のままで `text-highlight-text` に統一していない。仕様A.4はカテゴリ見出しへの追加のみ要求しており範囲外だが、視覚的一貫性の観点で気になるなら合わせてもよい。

確認した主要点:
- catalog-build.test.mjs の2件の書き換えは、実装（masthead共有・トップ1カラム化・component link削除）と整合。裁定済みの契約変更として妥当。
- `data-site-*` の値なしJSX属性は React で `="true"` にレンダリングされ、`data-site-masthead="true"` 単一一致など全カウント/正規表現がdocumentation-home.tsx / documentation-shell.tsx / site-masthead.tsx の実装と一致する。
- 変更していない既存テスト「トップがLP要素なしで3経路とtoken置換を順に案内する」の `elchika-inc/ui` は `<title>` 要素でヒットするため、hero文言変更後も壊れない。
- `mb-16` がsticky wrapper内のsubnavに残る点はmargin collapseで実害なし、仕様も移動を任意としている。
- 値系arbitrary valueの新規混入なし（`[scrollbar-width:none]` はproperty形式で許容範囲）。`text-xs`・旧tracking名・数値leadingの新規使用なし。
- ブランド表記（「elchika.inc」不使用）、hero文言固定、eyebrow配色（`text-highlight-text`）は仕様・裁定通り。
===REVIEW_END===

````

### Security

````text
===REVIEW_BEGIN===
結論: LGTM

確認した主要点を短く記載。
- 差分内の描画対象はすべて静的 JSX（固定文字列・固定配列 `["install","tokens","components"]`・`"light"|"dark"` union の `{nextTheme}`）で、ユーザー入力や外部データを反映する経路がなく、`dangerouslySetInnerHTML` 等の危険な sink も新規追加されていない。
- 新規追加の `<a href>` は `/`・`/components/`・`/components/button/`・`#install|#tokens|#components` の同一オリジン固定パスのみで、`target="_blank"` の新規追加もないため reverse tabnabbing の懸念は生じない。既存の GitHub 外部リンク（`sourceHref`）は本レンズの差分外で不変。
- テーマ永続化（`applyTheme` / `readTheme` / `localStorage` キー `elchika-ui-theme`）のロジックは変更されておらず、書き込まれる値は型で `"light"|"dark"` に制約されている。
- `component-index.tsx` / `component-documentation.tsx` の iframe 読み込み部分は今回の差分に含まれず不変。
- `scripts/catalog-build.test.mjs` の更新2件はビルド済み静的 HTML に対する assert のみで、新たな外部実行・ネットワークアクセス・ファイル書き込みを追加していない。
===REVIEW_END===

````

### Core Logic

````text
advisor の確認内容も踏まえ、資料の範囲でレビューを確定します。

===REVIEW_BEGIN===
結論: LGTM

[OPTIONAL] scripts/catalog-build.test.mjs（未変更テスト「トップがLP要素なしで3経路とtoken置換を順に案内する」）: `html.indexOf("elchika-inc/ui")` が wordmark の `elchika-inc<span>/ui</span>` 分割により本文ではなく `<title>` にしかマッチしなくなっている（テスト自体はpassするが検査意図が形骸化）。`elchika-inc<span` へ変更すると本文の位置検査として機能する。
[OPTIONAL] scripts/catalog-build.test.mjs（新テスト、component側）: `/components/button/` に対して `data-slot="sidebar"` が1件であることを追加検証すると、裁定「Sidebar件数はサイト用1件」を静的側でも安価に固定できる（§7 Playwright と重複するため必須ではない）。
[OPTIONAL] src/site/component-index.tsx:153付近: カテゴリ見出しブロックの親が `items-baseline` のまま。左側divの1行目がeyebrow（11px）になったため、右の「N items」がh2ではなくeyebrowの行に視覚的に揃う可能性がある。意図した見え方か実ブラウザで確認推奨（意匠観点でありCore Logicの欠陥ではない）。

確認した主要点:
- catalog-build.test.mjs の2件は、裁定どおり「トップは `/components/` への案内のみ」「masthead 共有 + component ページのみ docs shell」という新契約に更新されており、旧サイドバー前提のアサーション（`data-docs-shell` 必須、`aria-current` on `/` 等）は適切に home 側から除去されている。
- JSX属性順のSSR出力（`data-site-eyebrow` → `class`、`href="/"` → `class`）は各正規表現アサーションと整合し、誤マッチ・マッチ漏れは生じない。masthead抽出用の非貪欲+`s`フラグ正規表現も、同ページ内の他の `<header data-site-hero>` と衝突しない。
- `documentation-home.tsx` は `SiteMasthead` に `theme`/`onThemeChange` を渡していないが、`ThemeToggle` は `useSiteTheme` にフォールバックして自己完結するため、テーマ切替・`localStorage` 更新は機能する（トップに theme 依存のプレビューが無いため実害なし）。
- `data-site-header` ラッパーと `SiteMasthead` 自身がともに `sticky top-0` を持つ二重指定は、position:sticky の入れ子として無害かつ仕様A.1/A.3双方の要求どおり。
- 値系 arbitrary value の新規混入なし（`[scrollbar-width:none]` は property形式で正規表現対象外）。`typography-usage.test.mjs` が縛る新規 `text-xs`・旧 tracking 名・数値 `leading-N` の使用は無い。
- `text-highlight-text` の変数解決、Sidebar件数の裁定範囲、footer/証跡命名等の既確定事項には触れていない（再提起せず）。
===REVIEW_END===

````

### Tests

````text
===REVIEW_BEGIN===
結論: LGTM

[OPTIONAL] scripts/catalog-build.test.mjs:213 / トップページ本文の wordmark が `elchika-inc<span>/ui</span>` に分割されたため、「トップがLP要素なしで…」テストの `html.indexOf("elchika-inc/ui")` は本文ではなく `<title>` 一致に依存する形になった。テストの意図（本文ブランド表記の順序検査）が弱まっているので、次に触る際は `<title>` 由来である旨をコメント等で明示するか、本文側の別 anchor に差し替えると良い。

[OPTIONAL] src/site/documentation-shell.tsx / `DocumentationShell` の `home` prop は、トップページが `DocumentationShell` を使わなくなったことで呼び出し側から到達不能になった（旧テストの `aria-current="page" href="/"` 検査がその唯一の被覆だったが、今回の diff で被覆ごと消えた）。dead prop の整理は実装側の別 issue として検討可。

確認した主要点:
- 2テストとも裁定どおり「削除・skipせず契約更新」「個別ページ側ループ維持」を満たす。`test(` 件数は rename のみで前後同数、rubric #3 の「削除行なし」要件に抵触しない。
- `data-site-masthead/subnav/hero="true"` は React/Fizz が `data-*` boolean を必ず文字列化する仕様（client・SSR 共通）に基づく確定挙動で、shorthand JSX からの `="true"` 期待は正しい。
- `masthead[1]` 抽出の非貪欲正規表現は、masthead の `</header>` が hero の `<header>` より先に閉じるため正しく機能する。`href="\/components\/"` は末尾クォートを要求するため `/components/button/` に誤マッチしない。
- ループ内に残る `/uppercase">Block</` は `data-site-eyebrow` が `className` より前に置かれている現状の属性順に依存するが、diff の実装と整合している。
- 「Sidebar件数はサイト用1件」の裁定は Playwright(rubric #7) の `/components/` 一覧ページの話であり、このユニットテストは `components/button` を対象とするため無関係で、混同していない。
===REVIEW_END===

````

### Domain

````text
advisorの指摘を踏まえ、確信度80%以上のFLAG候補は見当たらないため、LGTM＋OPTIONALでレビューをまとめます。

===REVIEW_BEGIN===
結論: LGTM

[OPTIONAL] src/site/component-index.tsx:157 / 確信度 60% / カテゴリ見出しの `<div>` に eyebrow（`section`）を挟んだことで `items-baseline` の基準行が eyebrow 行に変わり、右側の「N items」がカテゴリ名の行ではなく eyebrow の行に揃う。視覚的な軽微退行。 / `items-baseline` を `items-end` に変更。

[OPTIONAL] src/site/theme-toggle.tsx:44 / 確信度 50%（Button 実装が本レンズの資料外のため未確定） / `variant="outline"` の shadcn 標準ホバー・dark 背景（`hover:bg-accent` 等）が `bg-transparent` に勝つ場合、正本が求める「透明背景・hover で文字/枠だけ濃くなる」ピル型ボタンの質感から外れる。 / 実ブラウザ確認のうえ必要なら `hover:bg-transparent dark:bg-transparent hover:text-foreground` を追記。

[OPTIONAL] src/site/component-index.tsx:141 / 確信度 55% / `/components/` ページ冒頭の見出しラベル「Component index」が `text-primary` のままで、同ページ内の各カテゴリ eyebrow（`text-highlight-text`）と表記トーンが混在する。spec A.4 はカテゴリ見出しへの eyebrow 追加のみを明示しており、この箇所の変更までは指示していないため、worker 判断での変更ではなく司令塔へ申告を推奨（自己判断で変更しない）。

[OPTIONAL] src/site/documentation-shell.tsx:34 / 確信度 60% / `DocumentationShell` の `home` prop が呼び出し元ゼロ（`documentation-home.tsx` が独立レイアウト化したため）になり、未使用の分岐（`isActive={home}` 等）が残る。 / 呼び手が本当に無いことを確認のうえ `home` prop と関連分岐を削除。

確認した主要点:
- catalog-build.test.mjs の新旧2テストは、React が出力する `data-site-*="true"` 属性や JSX 属性順（`data-site-eyebrow` → `className`）と整合しており、正規表現は実装 DOM に対して意図どおりマッチする。
- トップページから個別 component への列挙リンクを外し `/components/` への一覧導線検証へ置き換えた変更は、裁定済みの契約更新として実装（`Button render={<a href="/components/" />}`）と整合している。
- ブランド表記（`elchika.inc` 不使用）、`text-highlight-text` の新規使用、Sidebar 0/1 件の使い分け（トップ0件・component一覧は別テストで検証）、`#install/#tokens/#components` の id と subnav href の対応、`main-content` の到達先、手順書追記（10行以内）は spec の記述と一致する。
- typography-usage.test.mjs に抵触する新規の `text-xs` ・旧 tracking 名・数値 `leading-N` の使用は無く、arbitrary value も property 形式 `[scrollbar-width:none]` のみで値系は無い。
===REVIEW_END===

````
