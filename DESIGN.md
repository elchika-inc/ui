# DESIGN — elchika inc. design system のブランド性格

standards の [`DESIGN.md`](https://github.com/elchika-inc/standards/blob/main/DESIGN.md) が正本。このファイルは standards §12 が定める **「既定の意図の置き場所」** として、各プロダクトが継承するハウススタイルを記録する。

当リポジトリはプロダクトではなく **design system そのもの**なので、§12 のテンプレ（ノブを回したか）を埋めるのではなく、**プロダクトが継承する既定の側**を書く。

## 意図の正本は design-tokens.html

**なぜその色か・なぜその書体か・なぜその制約かは、すべて [`src/styles/design-system/design-tokens.html`](src/styles/design-system/design-tokens.html) の「決めたことと、その理由」が正本。** 約25件の決定が理由付きで残っている。

**このファイルに理由を書き写さない。** 同ページが「トークンを更新するときは、ここに追記してください」と宣言しており、複製すると追記先が2つになってドリフトする。`tokens.css` / `brands.css` は `build-tokens.mjs` の生成物なので、値の正本も同ページ。

このファイルが持つのは、正本に**書かれていないもの**だけ:

- プロダクト側が「自分の `DESIGN.md` に何を書くか」を判定するための入口
- design system として避けるもの（正本では各決定に分散している）
- 再検討トリガー（正本にほぼ無い）

## プロダクトが継承する既定

| 面 | 既定 | 詳細 |
|---|---|---|
| ブランドカラー | 青がメイン、黄がサブ。**全プロダクトがコーポレートの青を継承する** | 色相をプロダクトごとに振り分ける案は検討のうえ不採用。プロダクトの区別は**ワードマークとプロダクトマーク**が担当する |
| アクセント（黄） | コーポレート固定。プロダクトは変えない | 「別プロダクトだと分かるが、同じ会社に見える」状態を作っている面 |
| ステータス色 | 上書き禁止 | ブランド表現ではなく、安全に関わる語彙として扱う |
| 書体 | IBM Plex スーパーファミリー。Mono は「ラベルと数値」専用レイヤー | `--font-display` と `--font-body` は同一。`:lang(en)` が和欧の並び順と組版を切り替える |
| 密度・余白・ニュートラル | 共有 | プロダクトごとに変えない |

**上書きしてよい面は `--brand-*` の 5 個だけ**（1プロダクト＝5行）。セマンティック層はその参照だけで値が決まる構造になっており、プロダクトが直接触るとライト・ダーク・状態の組み合わせで定義が増え続けて破綻する。

色相の予備（hue reserve）は「使わない」だけで「使えない」わけではない。色による識別が必要になったときに構造から作り直さずに済ませるために残してある。**引くときは正本の色差データを読むこと** — 目視で選ぶと衝突する（当初候補の teal / azure は success の緑と ΔE 3.1 まで接近しており、実測で除外された）。

## 避けるもの（design system として）

正本の各決定に分散しているものを、禁止事項として1箇所に集約する。理由は正本を参照。

- **生成物（`tokens.css` / `brands.css`）を直接編集する** — `design-tokens.html` を編集して `build-tokens.mjs` で再生成する
- **黄の背景に青文字を乗せる** — 小面積で違和感が出る。黄背景には黄系の濃色を重ねる
- **`p-7` / `font-bold` のような系外の値を書く** — Tailwind の spacing / fontWeight / screens は extend でなく上書きしてある。デフォルトを残すとトークンが形骸化する
- **チャートの系列線にステータス色を使う** — その系列自体が「エラー」に見える。ステータス色は帯にだけ使う
- **チャートの系列を6つ以上に増やす** — 5が上限。増えたら色を足さず、凡例をやめて線に直接ラベルを置く
- **色だけでステータスを伝える** — 記号（✓ ! × i）と形（円・角丸・菱形）を必ず併用する
- **和欧の間に手動でスペースを入れる** — 四分アキは組版の仕事
- **既存プロダクトの現行テーマに寄せる** — トークン側を正とし、プロダクト側を寄せる

## 再検討トリガー

以下が起きたら、上の既定を見直す。

- **6色目のプロダクト色相が必要になったとき** — indigo は青との最悪ケースが ΔE 7.4 で1型・3型では見分けがつかず、在庫にはあるが実質使えない。6色目の要求は「色でプロダクトを識別すること自体をやめる」サインとして扱う（正本に明記された唯一のトリガー）
- **プロダクトの区別がユーザーに伝わっていないと分かったとき** — 全プロダクトが同じ青を継承する構成は、識別をワードマーク／プロダクトマークに全振りしている。マーク側が機能していない場合、色の方針ではなくマークの設計を先に疑う
- **プロダクトが `--brand-*` 以外を上書きしたくなったとき** — 上書き面を5個に絞った前提が崩れている。プロダクト側で回避せず、design system への変更提案として持ち込む

> **未確定**: 上記以外の再検討トリガー（書体・密度・ステータス色をいつ見直すか）は、正本にも判断の記録が無い。必要になった時点で決めて正本と本ファイルの双方へ追記する。**空の項目を埋めるために推測でトリガーを書かない** — 見直し条件は判断であり、判断していないものを書くと、していない判断をしたことにしてしまう。

## ブランドボイス

正本の「文言も設計材料として記録する」が定める3軸に従う。

- エラーは謝らない。何が起きたかと、次に何をすればいいかを書く
- ボタンの動詞と結果の文言を一致させる
- 実装の都合ではなく、利用者が操作している対象の名前で呼ぶ

文章のトーンの原則は standards §10 が正本（「売り込まない、価値を見せる」）。

## プロダクト側が書くこと

各プロダクトの `DESIGN.md` には、standards §12 の MUST 3項目（ノブの決定 / 避けるもの / 再検討トリガー）を書く。**このファイルの既定を追認する行は書かない** — 既定どおりなら「既定に従う」の一行でよい。
